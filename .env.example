# This project has two separate env files - copy each one into place:
#
#   server/.env.example  ->  server/.env
#   client/.env.example  ->  client/.env
#
# See README.md for what each variable does.
